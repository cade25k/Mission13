@{
    ViewData["Title"] = "Home Page";
}

@model List<Bowler>

<h1>Bowlers</h1>

<ul>
    @foreach (Bowler b in Model)
    {
        <li>@b.BowlerID.ToString() - @b.BowlerFirstName @b.BowlerLastName</li>
    }
</ul>